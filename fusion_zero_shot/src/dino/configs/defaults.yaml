# Default paths relative to the dino/ directory.  Override any entry by
# creating configs/local.yaml (ignored by git).
paths:
  data_root: data
  experiments_root: experiments
  outputs_root: outputs
  assets_root: assets
  cache_root: outputs/cache
  logs_root: outputs/logs
  umd_label_root: null
model:
  repo_dir: third_party/dinov3
  checkpoint_path: null  # Set in configs/local.yaml or DINO_CHECKPOINT_PATH
