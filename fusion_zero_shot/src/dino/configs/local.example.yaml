# Copy this file to configs/local.yaml and edit it for machine-specific paths.
# Keep configs/local.yaml untracked.
model:
  checkpoint_path: /absolute/path/to/dinov3_vit7b16_pretrain_lvd1689m.pth
paths:
  # Uncomment to override dataset locations, e.g. when mounting elsewhere.
  # data_root: /mnt/datasets/affordance/dino/data
  # umd_label_root: /mnt/datasets/UMD/part-affordance-dataset/tools
